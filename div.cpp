#include<bits/stdc++.h>
using namespace std;
int main()
{
       int a, b;
       cin>>a>>b;
       if(b!=0)
       {
              cout<<"Division of two number: "<<a/b<<endl;
       }
       else
       {
              cout<<"Error: Denominator is zero"<<endl;
       }
       return 0;
}